import style from "./HomeHeroSection.module.css";

const HomeHeroSection = () => {
  return (
    <section
      className={`${style.gradient} relative z-20 bg-secondary h-screen w-full `}
    >
      <div></div>
    </section>
  );
};
export default HomeHeroSection;
