import style from "./footer.module.css";

const Footer = () => {
  return (
    <footer className={`${style.containerFooter} w-full`}>
      <section className="max-width-custom m-auto h-full"></section>
    </footer>
  );
};

export default Footer;
